<h2> Mis Cartas a la Venta </h2>

<div class="row-fluid">
  <div class="span6">
    <table class="table">
      <thead>
      	<tr>
        	<th>Carta</th> 
        	<th>Precio de venta</th>
      	</tr>
      </thead>
      <tbody>
      	<% @sale.each do |sale| %>
      		<tr>
        		<td></td>
         		<td><%= sale.starting_price %> €</td>
         		<td><%= link_to "Ver", sale_path(sale), :class => "btn btn-small btn-info" %></td>
        		<td><%= link_to "Editar", edit_sale_path(sale), :class => "btn btn-small btn-warning" %></td>
         		<td><%= link_to 'Destroy',sale_path(sale), method: :delete, data:{confirm:'Are you sure?'}, :class => "btn btn-small btn-danger" %></td>
      		</tr>
       	<% end %>
      </tbody>
    </table>
  </div>
</div>

<div class="row-fluid">
  <div class="span1"><%= link_to 'Inicio', root_path, :class => "btn" %>
  </div>
  <div class="span2"><%= link_to 'Añadir Venta', new_sale_path, :class => "btn btn-success" %>
  </div>  
</div>