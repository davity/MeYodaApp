<h1> Nueva Carta a la Venta </h1>

<h2> Introduce el Precio Inicial de tu carta: </h2>

<%= form_for @sale, :url =>sales_path(@sale, :card_id => @card.id) do |f| %>
  
  <% if @sale.errors.any? %>
  
	<div id="errorExplanation">
    	<h2><%= pluralize(@sale.errors.count, "error") %> 
	prohibited this post from being saved:</h2>
    
	<ul>
	<% @sale.errors.full_messages.each do |msg| %>
      		<li><%= msg %></li>
    	<% end %>
    	</ul>
  
	</div>

  <% end %>

  <p>
    <%= f.label :starting_price %><br>
    <%= f.number_field :starting_price %> €
  </p>
  <p>
    <%= f.submit %>
  </p>
  

<% end %>

<%= render "form" %>
